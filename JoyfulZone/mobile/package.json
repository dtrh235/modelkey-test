{
  "name": "joyful-zone-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Joyful Zone — Capacitor Android 壳",
  "scripts": {
    "prepare:www": "powershell -ExecutionPolicy Bypass -File scripts/prepare-www.ps1",
    "cap:sync": "npm run prepare:www && npx cap sync && powershell -ExecutionPolicy Bypass -File scripts/patch-android-http.ps1",
    "cap:android": "npm run cap:sync && npx cap open android",
    "cap:run": "npm run cap:sync && npx cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
