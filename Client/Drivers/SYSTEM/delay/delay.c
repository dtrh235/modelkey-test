/**
 ****************************************************************************************************
 * @file        delay.c
 * @author      Mr.Mr.
 * @version     V1.1
 * @date        2023-02-25
 * @brief       ???SysTick??????????????????��???(???ucosii)
 *              ??delay_init??????????? delay_us??delay_ms?????????
 * @license     Copyright (c) 2022-2032,  
 ****************************************************************************************************
 * @attention
 *
 * ?????:ST-1 STM32F407?????
 *  
 *  
 * ??????:www.genbotter.com
 * ??????:makerbase.taobao.com
 *
 * ??????
 * V1.0 20230206
 * ????��???
 * V1.1 20230225
 * ???SYS_SUPPORT_OS???????, ???????UCOSII 2.93.01?��, ????OS??��????
 * ???delay_init???????8???,????????MCU???
 * ???delay_us?????????????, ????OS
 * ???delay_ms??????delay_us??????.
 *
 ****************************************************************************************************
 */

#include "./SYSTEM/sys/sys.h"
#include "./SYSTEM/delay/delay.h"
#include "app_config.h"

#if (APP_USE_FREERTOS == 1)
#include "FreeRTOS.h"
#include "task.h"
#endif


static uint32_t g_fac_us = 0;       /* us????????? */

#if (APP_USE_FREERTOS == 1)
static uint8_t delay_rtos_can_sleep(void)
{
    if(xTaskGetSchedulerState() != taskSCHEDULER_RUNNING) {
        return 0u;
    }
    if((SCB->ICSR & SCB_ICSR_VECTACTIVE_Msk) != 0u) {
        return 0u;
    }
    return 1u;
}
#endif

/* ???SYS_SUPPORT_OS??????,???????OS??(??????UCOS) */
#if SYS_SUPPORT_OS

/* ??????????? ( ucos??????) */
#include "os.h"

/* ????g_fac_ms????, ???ms??????????, ????????????ms??, (???????os?????,??????) */
static uint16_t g_fac_ms = 0;

/*
 *  ??delay_us/delay_ms??????OS??????????????OS????????????????
 *  ??????3??????:
 *      delay_osrunning    :??????OS??????????????,????????????????????
 *      delay_ostickspersec:??????OS?څ????????,delay_init????????????????????systick
 *      delay_osintnesting :??????OS?��???????,????��????��???????,delay_ms???��????????????????
 *  ?????3??????:
 *      delay_osschedlock  :????????OS???????,???????
 *      delay_osschedunlock:???????OS???????,???????????
 *      delay_ostimedly    :????OS???,???????????????.
 *
 *  ?????????UCOSII?????,????OS,?????�Ӧ�??????
 */

/* ???UCOSII */
#define delay_osrunning     OSRunning           /* OS??????��??,0,??????;1,?????? */
#define delay_ostickspersec OS_TICKS_PER_SEC    /* OS??????,??????????? */
#define delay_osintnesting  OSIntNesting        /* ?��???????,???��??????? */


/**
 * @brief     us??????,??????????(??????us?????)
 * @param     ??
 * @retval    ??
 */
void delay_osschedlock(void)
{
    OSSchedLock();                      /* UCOSII????,??????????????us??? */
}

/**
 * @brief     us??????,??????????
 * @param     ??
 * @retval    ??
 */
void delay_osschedunlock(void)
{
    OSSchedUnlock();                    /* UCOSII????,??????? */
}

/**
 * @brief     us??????,??????????
 * @param     ticks: ??????????
 * @retval    ??
 */
void delay_ostimedly(uint32_t ticks)
{
    OSTimeDly(ticks);                               /* UCOSII??? */
}

/**
 * @brief     systick?��??????,???OS????
 * @param     ticks : ??????????  
 * @retval    ??
 */  
void SysTick_Handler(void)
{
    /* OS ???????,????????????????? */
    if (delay_osrunning == OS_TRUE)
    {
        /* ???? uC/OS-II ?? SysTick ?��?????? */
        OS_CPU_SysTickHandler();
    }
    HAL_IncTick();
}
#endif

/**
 * @brief     ???????????
 * @param     sysclk: ????????, ??CPU???(rcc_c_ck), 168MHz
 * @retval    ??
 */  
void delay_init(uint16_t sysclk)
{
#if SYS_SUPPORT_OS                                      /* ?????????OS */
    uint32_t reload;
#endif
    g_fac_us = sysclk;                                  /* ??????HAL_Init?????systick????????????????????????????? */
#if SYS_SUPPORT_OS                                      /* ?????????OS. */
    reload = sysclk;                                    /* ????????????? ??��?M */
    reload *= 1000000 / delay_ostickspersec;            /* ????delay_ostickspersec?څ??????,reload?24��
                                                         * ?????,????:16777216,??168M??,???0.09986s????
                                                         */
    g_fac_ms = 1000 / delay_ostickspersec;              /* ????OS??????????????�� */
    SysTick->CTRL |= 1 << 1;                            /* ????SYSTICK?��? */
    SysTick->LOAD = reload;                             /* ?1/delay_ostickspersec???��???? */
    SysTick->CTRL |= 1 << 0;                            /* ????SYSTICK */
#endif 
}

/**
 * @brief     ???nus
 * @note      ??????????OS, ?????????????????us???
 * @param     nus: ??????us??
 * @note      nus????��: 0 ~ (2^32 / fac_us) (fac_us???????????, ???????????)
 * @retval    ??
 */
void delay_us(uint32_t nus)
{
    uint32_t ticks;
    uint32_t told, tnow, tcnt = 0;
    uint32_t reload = SysTick->LOAD;        /* LOAD??? */
    ticks = nus * g_fac_us;                 /* ?????????? */
    
#if SYS_SUPPORT_OS                          /* ?????????OS */
    delay_osschedlock();                    /* ???? OS ??????????? */
#endif

    told = SysTick->VAL;                    /* ?????????????? */
    while (1)
    {
        tnow = SysTick->VAL;
        if (tnow != told)
        {
            if (tnow < told)
            {
                tcnt += told - tnow;        /* ??????????SYSTICK?????????????????????? */
            }
            else
            {
                tcnt += reload - tnow + told;
            }
            told = tnow;
            if((tcnt & 0xFFFFu) == 0u) {
                IWDG->KR = (uint16_t)0xAAAAu;
            }
            if (tcnt >= ticks) 
            {
                break;                      /* ?????/????????????,????? */
            }
        }
    }

#if SYS_SUPPORT_OS                          /* ?????????OS */
    delay_osschedunlock();                  /* ??? OS ??????????? */
#endif 

}

/**
 * @brief     ???nms
 * @param     nms: ??????ms?? (0< nms <= (2^32 / fac_us / 1000))(fac_us???????????, ???????????)
 * @retval    ??
 */
void delay_ms(uint16_t nms)
{
#if (APP_USE_FREERTOS == 1)
    if((nms != 0u) && (delay_rtos_can_sleep() != 0u)) {
        vTaskDelay(pdMS_TO_TICKS(nms));
        return;
    }
#endif

#if SYS_SUPPORT_OS  /* ?????????OS, ????????????os????????CPU */
    if (delay_osrunning && delay_osintnesting == 0)     /* ???OS?????????,??????????��?????(?��????��?????????) */
    {
        if (nms >= g_fac_ms)                            /* ???????????OS????????????? */
        {
            delay_ostimedly(nms / g_fac_ms);            /* OS??? */
        }

        nms %= g_fac_ms;                                /* OS???????????��???????,????????????? */
    }
#endif

    delay_us((uint32_t)(nms * 1000));                   /* ????????? */
}

/**
 * @brief       HAL?????????????????
 * @note        HAL???????????Systick???????????��?Systick???��?????????????????????
 * @param       Delay : ???????????
 * @retval      None
 */
void HAL_Delay(uint32_t Delay)
{
    while(Delay > 65535u) {
        delay_ms(65535u);
        Delay -= 65535u;
    }
    delay_ms((uint16_t)Delay);
}










